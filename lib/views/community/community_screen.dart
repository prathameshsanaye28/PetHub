import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class commmunityScreen extends StatelessWidget{
  commmunityScreen({super.key});

  @override
  Widget build(context)
  {
    return Scaffold(
      body: Column(
        children: [
          Text('Community Screen'),
        ],
      ),
    );
  }
}